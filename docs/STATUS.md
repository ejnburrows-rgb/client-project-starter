# NBO status

**Brand:** NBO — Novo Business Order

**Authoritative branch:** `main`

**Authoritative source:** root `index.html`

**Vercel project:** `nbo-website`

**Production alias:** `https://nbo-website.vercel.app`

## September 18, 2026 cleanup

- Confirmed the NBO website commits are already on `main`.
- Confirmed four recorded Vercel production deployments are `READY`.
- Retired the generic starter-template README/package identity.
- Promoted the finished site from `nbo-website/index.html` to the repository root.
- Normalized package identity to `nbo`.
- Added deterministic static build and verification scripts.
- Removed the runtime dependency on the Tucker Pool CRM repository for the NBO logo by using an inline NBO mark.
- Kept the current immigration-law-firm landing-page positioning and bilingual copy intact.

## Remaining naming cleanup

The GitHub repository slug is still `client-project-starter`. The connected GitHub tool available during this cleanup does not expose repository rename. Rename the repository to **NBO** (or `nbo` if GitHub normalizes the slug) when a GitHub repository-settings/CLI write path is available.

The Vercel project currently remains `nbo-website`; its public site branding is NBO.

## Finish-line checks

1. Verify the live production alias after the next deployment.
2. Connect a real owner-approved contact or scheduling destination before changing the review CTA into a submission/booking flow.
3. Do not claim successful contact, scheduling, or CRM delivery without a verified integration.
